# Smoke flow (iOS): launch the built-in Settings app — present on every iOS Simulator, so it runs
# with zero app install. Replace the appId + steps with your app's.
appId: com.apple.Preferences
---
- launchApp
- takeScreenshot: ios-settings
