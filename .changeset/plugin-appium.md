---
'@pwtap/create': patch
---

Register `@pwtap/plugin-appium` as a stable plugin in the scaffolder menu — mobile testing via Appium: a raw WebdriverIO session (`app: WebdriverIO.Browser`, no curated facade), Android (UiAutomator2) + iOS simulator (XCUITest), macOS-first. Add it with `npx create-pwtap add appium`.
