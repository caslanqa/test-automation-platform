---
'@pwtap/create': patch
---

Register `@pwtap/plugin-maestro` as a stable plugin in the scaffolder menu — mobile testing via Maestro with two mixable authoring styles (a Playwright-style imperative API and batch YAML flows), Android + iOS simulator, macOS-first. Add it with `npx create-pwtap add maestro`.
